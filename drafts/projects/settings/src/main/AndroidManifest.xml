<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.yourena.tpout.settings">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".custompref.MainActivity">
            <!--网络相关数据的-->
            <intent-filter>
                <action android:name="android.intent.action.MANAGE_NETWORK_USAGE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".official.MyPreferenceActivity">

        </activity>
        <activity android:name=".custompref.CustomPreferenceActivity">

        </activity>
        <activity android:name=".custompref.ChangeValueActivity">

        </activity>
        <activity android:name=".frag.fragnest.FragNestPreActivity">

        </activity>
        <activity android:name=".customview.MyCustomViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>